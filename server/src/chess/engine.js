'use strict';\n\n// Represent board as 8x8 array; pieces as simple codes (e.g. 'wP', 'bK').\nclass ChessGame {\n  constructor() {\n    this.board = this.createInitialBoard();\n    this.turn = 'white';\n  }\n\n  createInitialBoard() {\n    // TODO: return starting positions\n    return [];\n  }\n\n  isLegalMove(from, to) {\n    // TODO: implement per-piece rules and check prevention\n    return true;\n  }\n}\n\nmodule.exports = { ChessGame };\n