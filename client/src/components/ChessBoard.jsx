import React, { useState } from 'react';\n\nexport default function ChessBoard() {\n  const [board, setBoard] = useState([]); // TODO: init from props or server\n  const [selected, setSelected] = useState(null);\n\n  const handleSquareClick = (row, col) => {\n    // TODO: select piece or attempt move, then notify server\n  };\n\n  return (\n    <div className=\"board\">\n      {board.map((r, ri) => (\n        <div key={ri} className=\"rank\">\n          {r.map((sq, ci) => (\n            <button key={ci} onClick={() => handleSquareClick(ri, ci)}>\n              {sq}\n            </button>\n          ))}\n        </div>\n      ))}\n    </div>\n  );\n}\n